# Why CareerHub

## 문제 의식

개발자 취업 준비는 보통 노션, 스프레드시트, 메모장, 여러 개의 GitHub 레포로 흩어져서 관리된다. 학습 기록은 노션에, 프로젝트 목록은 이력서에, 지원 현황은 스프레드시트에 — 이렇게 흩어져 있으면 "내가 지금까지 뭘 준비해왔는지"를 한눈에 파악하기 어렵고, 그 데이터를 자기소개서나 면접 준비에 다시 써먹기도 번거롭다.

CareerHub는 학습 기록·프로젝트·지원 현황·이력서를 한 곳에 모으고, 그렇게 쌓인 데이터를 AI가 실제로 활용해서 자기소개서 초안과 예상 면접 질문을 만들어주는 걸 목표로 만들었다.

## 왜 이렇게 설계했는가

**AI 분석 결과를 지원 내역(Application)에 종속시키고, 매번 새로 쌓이게 했다** (`AiAnalysis` 모델이 기존 결과를 덮어쓰지 않고 계속 `create`만 함).
회사마다, 지원할 때마다 강조해야 할 포인트가 다르기 때문에 "하나의 범용 자기소개서"가 아니라 특정 지원 건에 묶인 결과물이어야 의미가 있다고 판단했다. 또 과거에 생성했던 버전들을 비교해볼 수 있어야 다음 지원 때 더 나은 버전을 고를 수 있다.

**프로젝트 기술스택은 DB엔 배열(`String[]`)로 저장하지만, 입력은 콤마로 구분된 문자열 하나로 받는다.**
저장 형태는 배열이어야 나중에 "기술스택으로 필터링/집계" 같은 기능을 붙이기 쉽지만, 사용자 입력 폼에서 태그 입력 UI까지 만들 여유는 없어서, `React, TypeScript, Node.js`처럼 한 줄로 받고 서버에서 `split(",")`로 변환하는 절충을 택했다.

**인증 토큰을 localStorage가 아니라 httpOnly 쿠키로 내려준다.**
localStorage에 JWT를 저장하면 XSS 공격으로 토큰이 그대로 탈취될 수 있다. httpOnly 쿠키는 JS에서 접근 자체가 안 되기 때문에, 개인정보(이력서, 자소서 초안 등)를 다루는 서비스 특성상 이 정도는 기본으로 가져가야 한다고 판단했다.

**AI는 Groq(Llama 3.3 70B)를 붙였다.**
OpenAI 대비 무료/저비용으로 충분히 빠른 응답 속도를 낼 수 있어서, 포트폴리오 단계에서 API 비용 부담 없이 실제 LLM 연동 경험을 쌓기에 적합했다.

**배포를 Vercel/Render 원클릭이 아니라 EC2 + Docker + GitHub Actions로 직접 구성했다.**
더 쉬운 선택지가 있었지만, 이력서에 "배포해봤다"라고 쓰려면 실제로 이미지 빌드, 컨테이너 오케스트레이션, 리버스 프록시, 인증서 발급까지 손으로 겪어봐야 한다고 생각했다. 실제로 이 과정에서 Docker/Alpine 네이티브 모듈 컴파일 문제, 플랫폼별 lock file 불일치, nginx 설정 실수로 인한 장애 등 실전에서만 마주치는 문제들을 겪고 고쳤다 — 이 과정 자체가 프로젝트의 일부라고 생각한다.

## 아직 부족한 부분 (의도적으로 숨기지 않음)

- 프론트엔드 테스트 커버리지가 없다 (백엔드 서비스 레이어만 테스트되어 있음)
- CI 파이프라인이 테스트를 게이트로 쓰지 않는다 (배포 전에 테스트를 강제로 통과시키지 않음)
- 모니터링/알림이 없어서 장애를 사람이 직접 확인해야 안다

이 항목들은 다음 작업으로 남겨두고 있다.
